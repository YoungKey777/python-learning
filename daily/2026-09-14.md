# 2026-09-14 学习日志

**学习时长**：__ 小时

## 09-13 断了一天

- `daily/` 里没有 09-13，把整个 `Knowledge/` 扫了一遍 —— **那天零文件改动**。不是"python 停了"，是整天没碰过手
- 打卡线：

```
09-08 ✓  09-09 ✓  09-10 ✓  09-11 ✓  09-12 ✓  09-13 ✗
```


## ✅ 今天学了什么

### 1. pandas 路线立好了（`pandas/pandas_note.md`）

- 材料定了：**pandas 官方 Getting Started Tutorials（2.3 版文档）** —— 10 篇，拆成 8 天，每天一篇
  - 为什么锁 2.3 版：本机 Python 3.10.9，pandas 3.0 要 ≥3.11 装不了；而官网默认打开的 "latest" 就是 3.0，**行为不一样，别照着写**
- 每天固定动作：**读那篇（十几分钟）→ 关掉网页 → 在自己数据上把 3 个关键操作重敲一遍 → 填小节 → push**
  - 关键词是**关掉网页**。抄代码不算学，重敲才算

### 2. 「pandas 怎么学」的结论 —— 它跟前 7 章不是一回事

| | 前 7 章 | pandas |
|---|---|---|
| 学的是 | **知识** | **动作** |
| 学会的标志 | 能复述概念 | **手上有没有肌肉记忆** |
| 主要风险 | 看不懂 | **看懂了，但写不出** |

- **一句话：知识能读懂，动作只能做会。** 所以 8 天路线的成败不在"走完"，在"每天那 3 个动作真的敲过"
- 四条心法：
  1. **REPL 是主场，不是编辑器** —— 别写 20 行再跑；`df.shape` → `df.columns` → `df.head()` 一行一行喂，每步先看形状
  2. **每个动作敲两遍**：titanic 一遍（教程的标准答案，验对错）+ 碳价一遍（自己的数据，验真会）—— 只有 titanic 通过 = 学会了教程，没学会 pandas
  3. **卡 15 分钟就喊 AI** —— 不是卡到自闭，也不是卡 30 秒就问
  4. **笔记形态要换** —— `python_note.md` 那种概念拆解在 pandas 会失控（200+ 方法抄不完）。改成三行：**任务 / 我用的方法 / 踩的坑**

### 3. Day 0：教程数据落盘 ✅

- 5 个文件进 `pandas/data/`（共 581 KB），**每个都实跑 `df.shape` 验过**：

| 文件 | 形状 |
|---|---|
| `titanic.csv` | (891, 12) |
| `air_quality_no2.csv` | (1035, 4) |
| `air_quality_no2_long.csv` | (2068, 7) |
| `air_quality_long.csv` | (5272, 7) |
| `air_quality_parameters.csv` | (7, 3) |

- **踩到的坑：教程里的 github 数据地址本机直连不通**，换 jsDelivr 镜像就好了
  - `github.com/pandas-dev/pandas/raw/main/` → `cdn.jsdelivr.net/gh/pandas-dev/pandas@main/`
  - 另一个坑：`air_quality_no2.csv` **第一次会超时，重试就 200** —— 别以为地址写错了

### 4. 环境核对（一个都不用装）

```
Python 3.10.9   pandas 2.3.3   numpy 2.2.6   matplotlib 3.10.9   ✓
```

## 💻 今天写了什么代码

- `pandas/data/` —— 5 个教程数据集（不是代码，但是 Day 1 的弹药）
- `pandas/pandas_note.md` —— 8 天路线 + 网络备忘 + 实测形状表（Day 0 已翻 ✅）
- `roadmap.md` —— 阶段 2 加了一行指向 pandas 笔记
- `.gitignore` —— 加了 `!pandas/data/*.csv` 例外

## 🤔 卡住的地方 / 没懂的

- **`.gitignore` 里那行 `*.csv` 差点误伤** —— 那条规则本意是拦"行情/碳价这种量大又有版权的数据"，但它是无差别拦截，pandas 教程的公开小数据也被一起挡了
  - 处理：加一行例外 `!pandas/data/*.csv`。**那条规则本身是对的，不用改**
  - 留个心眼：以后凡是往 `code/data/` 之外放数据，先 `git status` 看一眼有没有 `!!`

## 📌 明天计划

- **Day 1：01 数据结构 + 02 读写**（同一天消化，两篇都短）
  - 目标动作：`read_csv` → `head() / info() / describe()` → 写回 csv
  - 练习：拿 titanic 走一遍，再拿**碳价数据**走一遍
- 顺手在 `code/` 里建个 `pandas_day1.py`，把当天 3 个动作敲进去（不许复制粘贴）

---
*记录规则：学到什么写什么，不用凑字数。坚持比完美重要。*
