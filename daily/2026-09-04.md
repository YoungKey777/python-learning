# 2026-09-04 学习日志

**学习时长**：__ 小时（下午 + 晚上）

## ✅ 今天学了什么
- **5.3 深挖补完（实验打磨三境界）**：①元组锁框架不锁内容（座位焊死、乘客自由）②原地改 vs 换指向（带下标/切片的 `=` 是改车，裸 `=` 是换车）③切片赋值可换长度（外科手术）；`u[:] = []` 清空
- **空结构理论**：列表删光元组结构还在——`([], [3,2,1])` 不报错，"空盘子也是盘子"
- **垃圾回收 GC**：没人指的对象引用计数归零自动回收，Python 没有 C 的内存泄漏（`sys.getrefcount` 看计数牌）
- **元组 `+=` = 换新车**：元组无原地操作，`t += (3,)` 造新元组换指向（对比列表 `+=` 原地，id() 验证）
- **5.4 集合全套**：创建（`{}`/`set()`，空集合只能 set()）、三王牌（去重/O(1) 秒查/朋友圈四运算 `- | & ^`）、无序无索引、元素必须可哈希、集合推导式
- **哈希指纹机**：`hash()` 给对象发身份证号；可变对象算不出指纹 → 不能进集合；含列表的元组连坐（5.3 伏笔回收：从里到外全焊死）
- **5.5 字典全套**：键值对查表；四操作（存=新增/覆盖、取、del 删、in 查键不查值）；KeyError vs `.get()` 保命；键要可哈希、**值随便放**（字符串/列表/嵌套字典都行）；dict 保插入顺序
- **5.6 循环六工具**：items() 键值同拿 / enumerate() 带序号 / zip() 拉链配对 / reversed() 倒走 / sorted() 不动原序列 / 循环改列表→建新列表（NaN 数据清洗标准姿势）
- **dict_items 视图**：items() 返回实时镜头不是快照（字典变视图跟着变）
- 笔记大整理：5.2–5.5 全部按 5.6 格式重写为"一句话+口诀 → 编号分点（带代码）→ 量化收尾"（用户发现 5.4/5.5 顺序颠倒已修复）

## 💻 今天写了什么代码
- hello_python.py：切片赋值换长度实验、del 连续删（IndexError 现场）、`hash()` 指纹实验、嵌套元组、`.get()` 保命实验、items/enumerate/zip 遍历、NaN 清洗、去重实战（`len(set(代码列表))`）

## 🤔 卡住的地方 / 没懂的
- "元组里的列表删光了，元组结构会变吗？" → 不会崩（del 越界才会报错），删的是乘客不是座位——**空了结构也在**（已搞懂）
- "集合为什么不能放列表？因为元素重复？" → 不是！是**可变对象哈希会变**，柜子定位全乱（已搞懂）
- "字典的值可以不是数字吗？" → 值完全没限制，只有键挑食（可哈希）（已搞懂）
- 5.3 体检题：单元素元组 `(5,)`、负索引切片返回元组、多赋值左边不是元组、嵌套 len 只数外层（全通关）
- 信息量大有点记不住 → 正常，靠睡眠巩固 + 明天翻分点笔记复习

## 📌 明天计划
- 收尾第 5 章：5.7 条件补充（小）+ 5.8 序列比较（极短）
- **第 5 章总结** + 欠债的"数据类型全家福"总表（list/tuple/set/dict 五大家族横向对比）
- 进第 6 章：模块（import，量化里天天用——akshare/pandas 就是模块）

---
*记录规则：学到什么写什么，不用凑字数。坚持比完美重要。*
