# 2026-09-11 学习日志

**学习时长**：__ 小时（满课 + 组会日，晚上精力见底，只推进了 7.1）

## ✅ 今天学了什么

**7.1 更复杂的输出格式**
- **四种"写入值"的方式**：表达式语句 / `print()` / **文件对象的 `write()`**（`sys.stdout` 就是"标准输出"这个文件 → **`print()` 本质上是往文件里写**）/ 格式化输出
- **f-string** = 格式化字符串字面值（早就在用，今天知道正名）；`str.format()` 是上一代写法，自己不用但**读老代码要认得**；花括号里那串叫**格式规格**，骨架 `{:[填充][对齐][符号][宽度][,][.精度][类型]}`，**两代通用**
- **`str()` vs `repr()` —— 本节真正的干货**
  - `str()` 给人看 / `repr()` 给解释器看（可重建该对象）
  - 字符串的 repr 带引号 → **解答了"为什么在 REPL 里敲字符串会带引号"**：交互模式显示的是 `repr()`，`print()` 用的是 `str()`
- **repr 的实战价值：让"看不见的东西"显形** —— `\n` 显示成两个字符、空格一目了然 → 以后调数据用 `print(repr(x))`，比 `print(x)` 信息量大得多
- 附：格式规格速查表（千分位 / 百分比 / 带符号百分比 / 小数位 / 对齐 / 补零）
- 收尾两条：`string.Template`（老式 `$x`，知道有就行）、切片拼接和 `.ljust()`（已被格式规格取代）

## 💻 今天写了什么代码
- 今天没写实验代码（7.1 的四个实验留到明天和 7.2 一起跑）
- 课外：修好了 matplotlib（见下）

## 🔧 环境修复（今天的重头戏）

**坑 1：matplotlib 3.7.0 与 numpy 2.x 冲突**
- 症状：`import matplotlib` 直接崩 → `ImportError: numpy.core.multiarray failed to import`
- 原因：matplotlib 3.7 是 **numpy 1.x 时代**编译的，本机 numpy 是 2.2.6
- 修复：`pip install --upgrade matplotlib` → **3.10.9** ✅

**坑 2：中文画成方块**（顺手发现，周一画第一张图必然撞上）
- 症状：标题渲染成 `□□□□□`，警告 `Glyph 33541 missing from font(s) DejaVu Sans`
- 原因：默认字体 DejaVu Sans **不含汉字**
- 修复：建永久配置 `C:\Users\27182\.matplotlib\matplotlibrc`（`font.sans-serif: SimHei` + `axes.unicode_minus: False`）→ **以后任何脚本画图中文自动正常**，"换电脑怎么办"的临时方案也写进笔记了

**端到端实测通过 ✅（周一的实战预演）**
```
akshare 拉真实行情 → pandas 整理 → matplotlib 出中文图   全链路跑通
```
- 样本：`ak.stock_zh_a_hist(symbol="600519", adjust="qfq")` → 162 行 × 12 列
- 顺带认全了真实数据的列名：日期 / 股票代码 / 开盘 / 收盘 / 最高 / 最低 / 成交量 / 成交额 / 振幅 / 涨跌幅 / 涨跌额 / 换手率
- 两个坑的排查过程已记进笔记「环境备忘」

## 🤔 卡住的地方 / 没懂的
- 今天满课 + 组会，精力见底，只推进了 7.1 —— 学习节奏被课程挤压，属正常波动，不追进度
- 7.1 的实验（str/repr 对比 + 格式规格速查）顺延到明天，和 7.2 一起跑

## 📌 明天计划
- **7.2 读写文件** —— 今天的主菜顺延，`open()` + `with` 正好用上 8.8 刚学的
- **7.2.2 用 json 保存结构化数据**
- 周末：10 标准库 → 12 虚拟环境和包 → **周一 9/14 pandas 开箱** 🚀

---
*记录规则：学到什么写什么，不用凑字数。坚持比完美重要。*
*2026-09-08 立誓：拼量化高薪留北京，断更就是背叛自己——每天打卡不许停。*
