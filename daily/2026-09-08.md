# 2026-09-08 学习日志

**学习时长**：__ 小时（恢复日！断更 4 天回归）

## ✅ 今天学了什么
- **第 5 章毕业 🎓**：5.7 深入条件控制 + 5.8 序列比较全部收完，全章总结
- **5.7 条件控制完整规则**：三组武器（比较符 / `in` 成员检测 / `is` 身份检测）、链式比较 `a < b == c`、优先级 `not > and > or`（括号最大）、**短路求值**（and 遇假即停、or 遇真即停）、**短路返回最后一个求值的值**（`x or 默认值` 兜底模式、`0` 是假值不能靠 or 兜底）、**海象运算符 `:=`**（表达式内边赋值边判断，防 C 语言 `=`/`==` 事故）
- **5.8 序列比较（sorted 的幕后裁判）**：字典式顺序 = 查字典规则逐对比；第一对不同即定局（长度不参与）；前缀规则（比到一方走完 = 短的输）；嵌套序列递归比；异类乱比 TypeError；int/float 按数值混比；**能排序的前提 = 元素支持 `<`**（集合/字典只能 == !=）
- **zip 深度答疑（自己追问挖出来的）**：zip() 是惰性"拉链机"不是元组，吐的产品永远是二元组对；list()/dict()/tuple()/for = 自己选的容器（机器与仓库分离）；`dict(zip(...))` 平行列表变查表；items() ≈ zip(d.keys(), d.values())；`next()` 拉一口证明惰性
- **字典 vs 集合（我提问发现的真秘密）**：dict 的键层就是集合——同一套哈希柜引擎（CPython 底层同源）；dict = 集合 + 每个键挂个值（名单 vs 花名册）；`d.keys()` 支持集合四则运算；`in` 问字典 = 问键
- **第 5 章全家福总表**（还债）：家族树两条血脉（序列族 str/list/tuple 按号找座 vs 哈希族 set/dict 按名找座）+ 横向对比表 + 血缘关系（集合=脱了值的字典、列表=可变元组）+ 选型口诀

## 💻 今天写了什么代码
- hello_python.py：zip 配对/造字典/dict(zip) 实验、type() 看 zip 对象、短路实锤（喊话函数 and/or 早停）、or 兜底 0 的坑、海象 :=、序列比较验证（5.8）

## 🤔 卡住的地方 / 没懂的
- "zip 之后是列表还是字典？" → 容器自己选，zip 只负责配对比（已搞懂）
- "字典最外层是元组？" → 不是！字典按点名不按排队，`d[0]` KeyError（已搞懂，是自己口误重问）
- "字典最外层是集合？" → **是！** 键层 = 集合，同哈希引擎（这个理解超前，已写进笔记）
- 短路返回值懵了一下 → `and/or` 用作普通值时返回最后求值的参数，不是 True/False（已搞懂）

## 📌 明天计划
- **第 6 章 模块**（import：量化天天用 akshare/pandas，含 `__name__ == '__main__'`、别名、dir() 翻箱）
- 本周冲刺路线：6 模块 → 8 错误异常（try/except）→ 7 输入输出（文件/json）→ 10 标准库精华 → 12 虚拟环境+pip 装包；周内 Python 基础收口，下周一开 pandas/akshare

---
*记录规则：学到什么写什么，不用凑字数。坚持比完美重要。*
*2026-09-08 立誓：拼量化高薪留北京，断更就是背叛自己——每天打卡不许停。*
