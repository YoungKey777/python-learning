# 🗺 Python 学习地图（碎片时间版）

> 设计原则：**只学核心，用到再补**。
> 每个模块三行话：学什么 / 怎么练 / 为什么学它。
> 核心模块 = 必学；可选模块 = 用到时再回来补。

---

## 阶段 0：环境搭建（半天）🟢

- **学什么**：装 Anaconda（自带 Python + 常用库）、VS Code、git
- **怎么练**：跑通第一个程序 `print("hello python")`，学会在 VS Code 里运行、在终端用 conda
- **为什么**：工具不顺手，后面全是摩擦

## 阶段 1：Python 基础（1~2 周）⬜ 核心

- **学什么**：变量和类型（int/float/str/bool）→ 列表/字典/元组/集合 → 条件 if 和循环 for/while → 函数（参数、返回值）→ 文件读写 → 类的基础
- **怎么练**：每天 3~5 个小题，从"打印三角形"到"统计一段英文的词频"；不会的用 AI 问，但**必须自己敲一遍**
- **为什么**：这是所有后续的地基。这一阶段快不快决定后面的顺畅度
- **提示**：别陷进去背语法——数据类型和函数会了就直接进 pandas，用到再回来补（比如异常处理、装饰器）

## 阶段 2：numpy + pandas（2~3 周）⬜ 核心中的核心

- **学什么**：
  - numpy：数组（ndarray）、索引切片、向量化计算（别写 for 循环）
  - pandas：DataFrame 的筛选/排序/groupby/merge、缺失值处理、**时间序列**（DatetimeIndex、resample 重采样、shift 滞后、rolling 滚动）
- **怎么练**：找一份真实数据（akshare/tushare 拉的历史行情，或自己课题的数据），做：清洗 → 算日收益率 → 滚动 20 日均线 → 按年汇总
- **为什么**：分析工作 80% 的时间是"把数据摆成能算的样子"，就是 pandas
- **检验标准**：能不用查资料完成"读 CSV → 算收益率 → 画图"全流程

## 阶段 3：数据可视化（1 周）⬜ 核心

- **学什么**：matplotlib 的折线/柱状/散点/多子图、轴标签、图例、保存图片
- **怎么练**：把阶段 2 的结果画出来：价格曲线、收益率分布直方图
- **为什么**：会画一张讲故事的图，是分析能力的重要体现

## 阶段 4：机器学习（2~3 周）⬜ 核心

- **学什么**：sklearn 的回归（线性/岭/树）和分类、train_test_split 划分、交叉验证、评估指标（RMSE/准确率）
- **怎么练**：用你手头最熟的数据做"用过去 20 天特征预测明天涨跌"的完整流程
- **为什么**：预测建模是分析工作的核心能力。**先学 sklearn，别急着上深度学习**

## 阶段 5：时间序列与统计（2 周）⬜ 核心

- **学什么**：平稳性（ADF 检验）、自相关、ARIMA 建模（statsmodels）、残差分析
- **怎么练**：对一段真实时间序列建 ARIMA，预测并和真实值对比，画误差图
- **为什么**：凡是带时间戳的数据（行情、气象、遥感……）都要过这一关
- **可选（用到再补）**：GARCH 波动率建模、协整与配对交易

## 阶段 6：项目实战（持续，边学边做）⬜ 核心

- **学什么**：数据获取（akshare/tushare/yfinance）→ 简单策略回测（双均线、动量、均值回归）→ 绩效指标（年化收益、夏普比率、最大回撤）
- **怎么练**：完成 **1~2 个能跑通的小项目**放进 `code/`：
  1. 「趋势跟踪」：均线策略回测，输出绩效报告和图表
  2. 「配对交易」：相关系数 → 价差 → 开平仓规则
- **为什么**：把前面学的串成一个完整流程，是检验是否真会的最好方式

---

## 可选模块（用到再补，不用现在学）

| 模块 | 什么时候需要 |
|---|---|
| 装饰器/生成器/迭代器 | 写通用框架、优雅处理数据管道时 |
| 爬虫（requests/bs4） | 需要的数据 akshare/tushare 没有时 |
| SQL | 实习/工作中用数据库时 |
| PyTorch | 深度学习需求出现时 |
| 多线程/异步 | 大量数据并发下载时 |

---

## 📌 使用建议

1. **每天固定 1~2 小时**学习（阶段 2~5 的核心内容），碎片时间看本地图复习框架
2. **每天写笔记 + 跑代码**，笔记存在 `notes/`，代码存在 `code/`
3. **当天学完就 commit**（`push.bat`），坚持看得见
4. **阶段 1 可以借力 AI 加速**：让 AI 讲概念、出练习题，但代码必须自己敲
5. **遇到不懂先标记**，跳过去，用到时再回来——这是"只学核心"路线的关键纪律
